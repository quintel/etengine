# updates lft node with the negative user value
- key = input_2
- update_period = future
- factor = 100
- update_type =
- max_value = 100000
- min_value = 10
# start_value_gql is multiplied by factor
- start_value_gql = present:V(foo,demand)
- query = UPDATE(V(lft), preset_demand, USER_INPUT())
