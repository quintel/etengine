= simple_form_for @user, :html => {:class => 'form-stacked'} do |f|
  = f.input :name
  = f.input :email
  = f.input :password
  = f.input :password_confirmation
  = f.association :role
  = f.button :submit
