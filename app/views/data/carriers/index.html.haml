- breadcrumb "Carriers"

= render 'search_box'

.header Carriers

%table
  %thead
    %tr
      %th Id
      %th Key
  %tbody
    - @carriers.each do |c|
      %tr
        %td= c.id
        %td= link_to c.key, data_carrier_path(:id => c)
