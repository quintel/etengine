- breadcrumb link_to "Inputs", data_inputs_path
- breadcrumb @input.key
= render 'search_box'
.header
  input #{@input.key}

%strong{style: "color: red"} Inputs have to be edited through ETsource.

%table
  - Input::ATTRIBUTES.each do |attr|
    %tr
      %td= attr
      %td= @input.send attr
