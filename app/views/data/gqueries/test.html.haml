= render 'list'

#content
  .header
    Test Query

  - form_tag test_data_gqueries_path do |f|
    %p= text_area_tag :query, params[:query], :style => "width: 500px; height: 200px"
  
    %p= submit_tag 'check'

  %h3 Result
  - if @query
    = render 'result'
    %h2 Query Debug
    %h3 present
    %pre= Current.gql.debug_present(@query)
    %h3 future
    %pre= Current.gql.debug_future(@query)
  - else
    %em no query
