- breadcrumb "GQueries"
= render 'search_box'
= render 'subnav'
%table
  %thead
    %tr
      %th Key
      %th Unit
      %th Updated at
      %th Deprecated Key
  %tbody
    - @gqueries.flatten.each do |gquery|
      %tr
        %td= link_to highlight(gquery.key, params[:q]), data_gquery_path(:id => gquery.key)
        %td= gquery.unit
        %td= gquery.gquery_group.try(:group_key)
        %td= gquery.deprecated_key
  %tfoot
    %tr
      %td{:colspan => 9}= paginate @gqueries
