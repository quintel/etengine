
var settings = <%= (@gql_test_case.settings || '').inspect.html_safe %>;

describe("For country NL, end_year 2050", function() {
  beforeEach(function() { this.addMatchers(customMatchers); });

  // All the indivudual gquery test cases.
  // This will be probably be from @gql_test_case.test_cases [text-column]
  <%= @gql_test_case.instruction.html_safe %>
});

function run_tests() {
  jasmine.getEnv().addReporter(new jasmine.TrivialReporter());
  jasmine.getEnv().execute();
}

// Make an API call. When complete runs the test-suites
load_queries(settings, run_tests);
