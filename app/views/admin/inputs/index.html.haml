- title 'Sliders'

%p= link_to 'Create new slider', new_admin_input_path

%table
  %thead
    %tr
      %th.tal id
      %th.tal Name

  %tbody
    - @inputs.each do |input|
      %tr{cycles}
        %td= input.id
        %td= link_to input.name, edit_admin_input_path(input)
