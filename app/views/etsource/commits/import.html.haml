- breadcrumb link_to("ETSource", etsource_root_path)

.header Commit: #{@commit.message}

%h2 Commit imported, cache cleared, server kicked.

%p 
  %h4{style: "color: red"}
    NOW GO HERE:
    = link_to "verify if dashboard is working", etsource_commit_path(id: 'current')
  It might take 30 seconds until the server is properly kicked and updated values show up. 
